__pycache__/
*.py[cod]
.pytest_cache/
.cache/
.venv/
venv/
.DS_Store

# Data and derived artifacts stay out of git; the manifest and configs define them.
data/
results/
*.h5ad
*.rds
*.mtx
*.loom

# LLM run records: provenance for the manuscript. Keep the ones behind published claims,
# not every exploratory call.
runs/*.json
!runs/.gitkeep

# Claude Code machine-local settings
.claude/settings.local.json

# Provider credentials
.env
.env.local
!.env.example

# Deck build artifacts (the generator is versioned, the output is not)
deck/node_modules/
deck/package*.json
deck/*.pptx
deck/*.pdf
deck/deck.md
