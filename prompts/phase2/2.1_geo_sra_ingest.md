---
id: phase2.ingest
version: 1.0.0
phase: "2"
description: Generate/extend the GEO+SRA harvester for paired scRNA-seq + ST cancer datasets.
model: claude-opus-5
effort: high
max_tokens: 32000
variables:
  tumor_types: {required: true, description: "Comma-separated tumor abbreviations, e.g. LUAD,BRCA,CRC,GBM,HNSCC,PDAC"}
  existing_module: {required: false, default: "src/panspatial/ingest/geo_sra.py", description: "Path to the module being extended"}
  current_source: {required: true, description: "Current contents of the module (paste), or NONE for a fresh implementation"}
---
TASK: Data mining -- automated GEO/SRA ingestion and metadata mapping.

Target tumor types: {{tumor_types}}
Module under development: {{existing_module}}

Current source:
```python
{{current_source}}
```

Produce the module (full file, not a diff) using `Bio.Entrez` against the `gds` and `sra`
E-utilities. Requirements:

1. Query GEO for human cancer series holding scRNA-seq and spatial transcriptomics, and
   mark which series are *paired* (both modalities, same study) vs *matched* (same patient
   IDs recoverable from characteristics fields) vs *unpaired*. These are different levels
   of evidence and must not be collapsed into one flag.
2. Emit one record per series with: Dataset_ID, Platform_scRNA (10x v2/v3/v4, Smart-seq2,
   ...), Platform_ST (Visium, Visium HD, Xenium, MERFISH, CosMx, Slide-seq, ...),
   ST_resolution_um and ST_is_single_cell (derived from platform, not from the title),
   Tumor_Type, Treatment_Status, Sample_Count, Patient_Count, Has_Raw_Access
   (dbGaP/EGA-controlled vs open), Download_URL, and Evidence (the substring that drove
   each classification, for audit).
3. Verify file structure per series by listing the supplementary directory over HTTPS
   (`https://ftp.ncbi.nlm.nih.gov/geo/series/GSExxxnnn/GSEnnnnn/suppl/`) and checking for
   matrix / barcodes / features and for spatial coordinates or `tissue_positions`. Record
   what is missing rather than dropping the series.
4. Respect NCBI limits: <=3 req/s without an API key, <=10 with `NCBI_API_KEY`; token-bucket
   throttle, exponential backoff on 429/5xx, and a local on-disk cache keyed by query hash
   so re-runs do not re-hit the API.
5. Platform classification must be a table of regex -> label with the matched span retained,
   never an LLM call and never a bare keyword `in` test that would match "not Visium".

Return: the complete module, then a short table of the classification regexes you used.
