---
id: phase5.title_abstract
version: 1.0.0
phase: "5"
description: Titles + Nature-style abstract, constrained to verified results only.
model: claude-opus-5
effort: high
max_tokens: 16000
requires_verified_results: true
variables:
  core_finding: {required: true, description: "One-sentence core finding, as supported by the results table"}
  results_table: {required: true, description: "The verified numbers available for the abstract (effect sizes, n, FDR)"}
  n_tumor_types: {required: true, description: "Number of tumor types the claim is supported in"}
  limitations: {required: true, description: "Known limitations that constrain how strongly the claim can be phrased"}
---
TASK: Draft 5 titles and a 150-word Nature-style abstract.

Core finding: {{core_finding}}
Verified results available: {{results_table}}
Tumor types supporting the claim: {{n_tumor_types}}
Limitations constraining phrasing: {{limitations}}

Abstract structure: context -> knowledge gap -> methodology (pan-cancer single-cell +
spatial) -> key discovery (the spatial NK niche) -> mechanistic insight -> translational
significance.

Constraints:
- Every quantitative claim must trace to {{results_table}}. Anything not there is written
  as [TBD], never as a plausible-looking number.
- The verb must match the evidence. "Associated with" for observational spatial
  correlation; "drives"/"mediates" only where a perturbation experiment exists. If the
  functional data are not in the input, the abstract cannot say "driving" -- flag that
  substitution explicitly if the core finding as given overstates the evidence.
- After the abstract, list every claim in it alongside the specific input line supporting
  it. Any claim with no supporting line is called out.
